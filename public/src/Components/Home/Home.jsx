import Navbar from './Navbar';

import styled from 'styled-components';

const Home = () => {
	return (
		<Container>
			<Navbar />
			home
		</Container>
	);
};

const Container = styled.div`
	// padding-inline: 5rem;
`;

export default Home;
